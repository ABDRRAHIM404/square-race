{
  "name": "maze-runners",
  "version": "0.1.0",
  "description": "Maze Runners - HTML5 Canvas + Capacitor mobile game",
  "private": true,
  "license": "MIT",
  "scripts": {
    "start": "npx serve www",
    "cap:add:android": "npx cap add android",
    "cap:sync:android": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/core": "^8.4.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.1"
  }
}
